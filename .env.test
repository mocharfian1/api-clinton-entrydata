# Test Environment Variables
NODE_ENV=test
PORT=3001

# Database Configuration for Testing
# You can use a separate test database or mock the database
DB_HOST=localhost
DB_USER=test_user
DB_PASSWORD=test_password
DB_NAME=test_database
DB_DIALECT=mysql
